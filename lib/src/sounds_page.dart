import 'package:flutter/material.dart';

class SoundsPage extends StatelessWidget {
  const SoundsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Sounds'),),
      backgroundColor: Color(0xFF2A3D3A)
    );
  }
}